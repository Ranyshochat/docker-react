sudo: required
language: generic

services: 
  - docker


before_install:
  - docker build -t ranyshochat/docker-react -f Dockerfile.dev .


script:
  - docker run ranyshochat/docker-react npm run test -- --coverage
